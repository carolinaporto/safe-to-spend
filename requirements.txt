# Runtime dependencies (installed into the Vercel Python function).
# Dev/test tooling lives in requirements-dev.txt.

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.34.0
python-multipart==0.0.32  # CSV file uploads (>=0.0.31: multipart DoS CVEs)

# Data validation / settings
pydantic==2.10.4
pydantic-settings==2.7.1

# Database
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.13

# Auth
PyJWT==2.13.0  # >=2.12.1: several token-validation CVEs
bcrypt==4.2.1

# HTTP client (daily FX fetch)
httpx==0.28.1
